# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

source 'https://github.com/CocoaPods/Specs.git'
target 'FakeLoacationSample' do
    # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
    use_frameworks!

    pod 'GoogleMaps'
    pod 'GooglePlaces'
    # Pods for FakeLoacationSample

    target 'FakeLoacationSampleTests' do
        inherit! :search_paths
        # Pods for testing
    end

    target 'FakeLoacationSampleUITests' do
        inherit! :search_paths
        # Pods for testing
    end

end
